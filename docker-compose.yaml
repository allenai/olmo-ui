version: "3"
services:
  api:
    build: ./api
    volumes:
      - ./api:/api
      - ./api/config.json:/secret/config/config.json
      - ./api/service_account.json:/secret/config/service_account.json
    environment:
      # This ensures that errors are printed as they occur, which
      # makes debugging easier.
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=DEBUG
    ports:
      - 8001:8000
  ui:
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - ./src:/ui/src
      - ./public:/ui/public
      - ./styled-system/:/ui/styled-system
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - LLMX_API_URL
      - DOLMA_API_URL
      - ENABLE_MOCKING
      - IS_CORPUS_LINK_ENABLED
      - IS_ATTRIBUTION_SPAN_FIRST_ENABLED
      - ABSOLUTE_SPAN_SCORE
      - BUCKET_COLORS
      - IS_DATASET_EXPLORER_ENABLED
      - IS_PETEISH_MODEL_ENABLED
      - IS_MULTI_MODAL_ENABLED
      - IS_MODEL_CONFIG_ENABLED=${IS_MODEL_CONFIG_ENABLED:-true}
      - IS_COMPARISON_PAGE_ENABLED=${IS_COMPARISON_PAGE_ENABLED:-true}
  nginx:
    image: nginx:1.25.1-alpine
    volumes:
      - ./nginx/local.conf.template:/etc/nginx/templates/prod.conf.template
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 8080:8080
    environment:
      - USER_EMAIL=${USER_EMAIL:-"murphy@allenai.org"}
