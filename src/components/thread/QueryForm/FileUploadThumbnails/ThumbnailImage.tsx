import { styled } from '@mui/material';

export const ThumbnailImage = styled('img')({
    position: 'relative',
    objectPosition: 'center',
    objectFit: 'cover',
    width: '100%',
    height: '100%',
});
