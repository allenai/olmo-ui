# BigQuery Schema

This captures the schema for the BigQuery table we use to track events.
This makes it easy to recreate table(s) as as required.

The input can be copy-pasted into BigQuery's "new table" form.

